/** Width of the time / all-day label column in week and 3-day views (px). */
export const CALENDAR_TIME_GUTTER_PX = 36;

/** Height of one hour row in timed day columns (px). */
export const CALENDAR_HOUR_HEIGHT_PX = 36;

/** Minimum height of the all-day band in week / 3-day views (px). */
export const CALENDAR_ALL_DAY_ROW_MIN_PX = 28;

/** Days rendered on each side of today in the 3-day scroll strip. */
export const THREE_DAY_SCROLL_BUFFER_DAYS = 90;
